import { TagHighlightDirective } from './tag-highlight.directive';

describe('TagHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new TagHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
